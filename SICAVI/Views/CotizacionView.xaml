<Page
    x:Class="SICAVI.WinUI.Views.CotizacionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:SICAVI.WinUI.ViewModels"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls">

    <Page.DataContext>
        <vm:CotizacionViewModel/>
    </Page.DataContext>

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="Cotizaciones" FontSize="28" FontWeight="SemiBold"/>
            <TextBlock Text="Crea cotizaciones y conviértelas en facturas" Opacity="0.6"/>
        </StackPanel>

        <!-- Botones -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="10">
            <Button Content="➕  Nueva cotización"
                    Click="NuevaCotizacion_Click"
                    Style="{StaticResource AccentButtonStyle}"
                    Padding="14,7"/>
            <Button Content="💰  Convertir a factura"
                    Click="ConvertirAFactura_Click"
                    Padding="14,7"/>
            <Button Content="🗑  Eliminar"
                    Command="{Binding EliminarCommand}"
                    Padding="14,7"/>
            <Button Content="🔄  Actualizar"
                    Command="{Binding ActualizarCommand}"
                    Padding="14,7"/>
        </StackPanel>

        <!-- Error -->
        <InfoBar Grid.Row="2"
                 Severity="Error"
                 IsOpen="{Binding HayError}"
                 Message="{Binding MensajeError}"
                 IsClosable="True"/>

        <!-- Tabla -->
        <Border Grid.Row="3"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="12"
                Padding="12">
            <controls:DataGrid
                ItemsSource="{Binding Cotizaciones}"
                SelectedItem="{Binding CotizacionSeleccionada, Mode=TwoWay}"
                AutoGenerateColumns="False"
                CanUserSortColumns="True"
                GridLinesVisibility="Horizontal">

                <controls:DataGrid.Columns>
                    <controls:DataGridTextColumn Header="ID"
                                                 Binding="{Binding Id}"
                                                 Width="50"/>
                    <controls:DataGridTextColumn Header="Fecha"
                                                 Binding="{Binding Fecha, StringFormat='{}{0:dd/MM/yyyy HH:mm}'}"
                                                 Width="130"/>
                    <controls:DataGridTextColumn Header="Cliente"
                                                 Binding="{Binding Cliente.NombreCompleto}"
                                                 Width="*"/>
                    <controls:DataGridTextColumn Header="Items"
                                                 Binding="{Binding Detalles.Count}"
                                                 Width="70"/>
                    <controls:DataGridTextColumn Header="Total"
                                                 Binding="{Binding Total, StringFormat='{}{0:C0}'}"
                                                 Width="110"/>
                </controls:DataGrid.Columns>
            </controls:DataGrid>
        </Border>
    </Grid>
</Page>